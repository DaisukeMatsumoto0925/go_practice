{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "go-server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/server.go",
            "cwd": "${workspaceFolder}",
        }
    ]
}